#  https://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
spring:
  jpa:
#    show-sql: true
    open-in-view: false
#   Настройка для автоматической инициализации БД. отключил
#    hibernate:
#      ddl-auto: create
    properties:
      #  http://docs.jboss.org/hibernate/orm/5.3/userguide/html_single/Hibernate_User_Guide.html#configurations
      hibernate:
        format_sql: true
        default_batch_fetch_size: 20
        # https://stackoverflow.com/questions/21257819/what-is-the-difference-between-hibernate-jdbc-fetch-size-and-hibernate-jdbc-batc
        jdbc.batch_size: 20
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database-platform: org.hibernate.dialect.SQLServer2008Dialect
  datasource:
#    url: jdbc:postgresql://localhost:5432/system5DB
#    username: admin
#    password: admin

    url: jdbc:sqlserver://localhost:1433;database=System5DB
    username: SA
    password: Rabota123!
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver



#  data.rest:
#    basePath: /api
#    defaultPageSize: 20
#    returnBodyOnCreate: true


logging:
  level:
    root: WARN
    com.example: DEBUG

    org:
      springframework:
        security=debug:

          org:
            hibernate:
              SQL=debug:
  file:
    name: system5.log

server.servlet:
  encoding:
    charset: UTF-8 # Charset of HTTP requests and responses. Added to the "Content-Type" header if not set explicitly
    enabled: true # Enable http encoding support
    force: true
#server:
#  ssl:
#    key-alias: root_cert
#    key-store: /Users/aleksejlaskin/Library/Java/JavaVirtualMachines/openjdk-15.0.2/Contents/Home/lib/security/cacerts
#    key-store-password: changeit